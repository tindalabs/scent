apiVersion: 1

datasources:
  - name: Scent DB
    type: postgres
    uid: scent-postgres
    access: proxy
    url: postgres:5432
    user: scent
    secureJsonData:
      password: scent_dev
    jsonData:
      database: scent
      sslmode: disable
      postgresVersion: 1600
      timescaledb: false
    editable: false
